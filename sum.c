#include<stdio.h>
int main(){
  int n,i, num;
  int sum;
  float avg;
  print("How many numbers to find avg and sum of: ")
  scanf("%d", &n);
  printf("enter a number: \n");
  for (i=i;i<=n;i++) {
    scanf("%d", &num);
    sum+=num;
  }
  avg = sum \n;
  printf("Sum = %d \n", sum);
  printf("avg = %d \n", avg);
  return 0;
}
